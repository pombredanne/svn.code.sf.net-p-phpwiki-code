<?php // -*-php-*-
// rcs_id('$Id$');
global $HIDE_TOOLBARS;
if (!$HIDE_TOOLBARS) { ?>
<!-- The logo -->
<div id="logo" style="height: 51px; 
     background-image: url(<?=$WikiTheme->getImageURL('splice')?>); background-repeat: repeat-x;"
><a class="wikilink" 
    href="<?=WikiURL(HOME_PAGE)?>"
    ><img src="<?=$WikiTheme->getImageURL('logo')?>" id="logo-img"
      border="0" alt="<?=fmt(_("%s: %s"), WIKI_NAME, HOME_PAGE) ?>" align="right" /></a>
</div>
<?php } ?>
