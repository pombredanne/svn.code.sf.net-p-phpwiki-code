<?php // -*-php-*-
rcs_id('$Id$'); ?>
<?php // On MonoBook we do away with only GET urls, no form.
  $sp = HTML::Raw("&nbsp;"); $id = $user->UserName();
?>
<div class="portlet" id="p-personal"><div class="pBody">
 <ul>
  <?php if ($user->isAuthenticated()) { ?>
  <li id="pt-userpage"><?= WikiLink($id, 'auto', HTML($sp,$id,$sp)) ?></li>
  <li><?= HTML::a(array('href'=>'?auth[logout]=1'),$sp,_("Logout"),$sp) ?></li>
  <?php } else { ?>
  <!-- Login or Register -->
  <?php if ($id) { ?>
    <li id="pt-userpage"><?= WikiLink($id, 'auto', HTML($sp,$id,$sp)) ?></li>
  <?php } ?>
  <li id="pt-login"><?= HTML::a(array('href'=>'?auth[login]=1'),$sp,_("Login"),$sp) ?></li>
  <?php } ?>
 </ul>
</div></div>
