Date: Wed, 13 Feb 2002 20:50:00 -0500
Mime-Version: 1.0 (Produced by PhpWiki 1.3.2-jeffs-hacks)
X-Rcs-Id: $Id: InterWiki,v 1.3 2002-02-14 01:49:53 carstenklapp Exp $
Content-Type: application/x-phpwiki;
  pagename=InterWiki;
  flags="";
  pgsrc_version="2 $Revision: 1.3 $";
  markup=2;
  charset=iso-8859-1
Content-Transfer-Encoding: binary

Ce wiki accepte les liens InterWiki dans un style copié sur celui de ~UseModWiki. Les liens vers des pages d'autres wikis peuvent être réalisés sans avoir à connaître ou taper les URL complets, par exemple : MeatBall:InterWiki produira un lien vers la page nommée "~InterWiki" sur ~MeatballWiki.

La carte InterWiki est extraite du bloc <verbatim> dans l'CarteInterWiki. Cette page doit être verrouillée pour que cela fonctionne (de façon à interdire aux hackers la possibilité to nefariously change every interwiki link on the wiki to point to some evil URL.) If no map is found in InterWikiMap (or the page is not locked), PhpWiki will fall back to using the file lib/interwiki.map.

The InterWiki map file in use at PhpWiki can be viewed at [http://phpwiki.sf.net/interwiki.map] (lib/interwiki.map, in your phpwiki distribution). The map file is manually updated. It is based upon and periodically synchronized with the UseMod:InterMap, but is not quite identical. One notable difference from ~UseModWiki is that PhpWiki supports '%s' within the URLs in the map--see the entry for RFC for an example.

Not all of the entries in the InterWiki map are actual wikis. For example, these are just regular web sites: Dictionary:fungible, [JargonFile:Alderson loop], [IMDB:Roman Holiday], RFC:2822, and ISBN:020171499X.

The ''Category'' "InterWiki" moniker is a special entry which allows one to link a page to a Category page without creating a back-reference.  (Category:Category links to the ~CategoryCategory page, yet this page won't show up in the back-links listing of Category~Category.)

Le terme "InterWiki" est aussi un concept plus large. Voir PhpWiki:InterWikiSearch pour plus de liens.
----
DocumentationDePhpWiki